

main() {
  runApp(PerguntaApp());

}

class PerguntaApp extends class name extends StatelessWidget {
  //const name({super.key});

  //@override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Text('Olá Flutter!')
    );
  }
}